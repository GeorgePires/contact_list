<nav id="bg" class="navbar navbar-expand-md navbar-dark mb-4">
  <%= image_tag ('icon.svg'), class: 'logo-top' %>
  <%= link_to 'Contact List', root_url, class: 'navbar-brand' %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse"
    aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarCollapse">
    <div class="navbar-nav mr-auto">
    </div>
    <% if user_signed_in? %>
      <div class="col-md-2 text-end">
        <%= render "layouts/header_login" %>
      </div>
    <% else %>
      <div class="col-md-2 text-end">

        <%= link_to new_user_session_path, class: "link" do %>
          <button type="button" class="btn btn-outline-primary text-white">Login</button>
        <% end %>

        <%= link_to new_user_registration_path, class: "link" do %>
          <button type="button" class="btn btn-primary">Create an Account!</button>
        <% end %>
    <% end %>

    </div>
  </div>
</nav>

